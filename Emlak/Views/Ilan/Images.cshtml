
@{
    ViewBag.Title = "Images";
}

<div class="container">
    @{
        Html.BeginForm("Images", "Ilan", FormMethod.Post, new { enctype = "multipart/Form-data" });
        foreach (var item in ViewBag.ilan)
        {
            <h3>@item.IlanId   Numaralı İlan İçin Resim Ekle</h3>
            <table>
                <tr>
                    <td><i>Resim Seç</i></td>
                    <td><input type="hidden" name="id" value="@item.IlanId" /></td>
                    <td><input type="file" name="file" /> </td>
                    <td><input type="submit" value="Ekle" class="btn btn-primary" /></td>
                    
                    <td>&emsp;&emsp;@Html.ActionLink("İlan Listesi", "Index", null, new { @class = "btn btn-warning" })</td>
                </tr>
            </table>
        }
    }
    <br />
    <div class="row">
        @foreach (var item in ViewBag.rsml)
        {
            <div class="col-md-4">
                <div class="thumbnail">
                    <img src="~/Content/images/@item.ResimAd" height="250" width="250" />
                </div>
            </div>
        }
    </div>
</div>

